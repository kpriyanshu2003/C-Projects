// WAP to subtract a number from another number
#include <stdio.h>
int main(){
    int a, b, c;
    printf("Enter two numbers : ");
    scanf("%d %d", &a, &b);
    c = a - b;
    printf("Difference of the two numbers is : %d", c);
    return 0;
}